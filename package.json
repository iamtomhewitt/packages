{
  "name": "@iamtomhewitt/packages",
  "version": "1.0.0",
  "author": "Tom Hewitt",
  "private": true,
  "scripts": {
    "build": "npm run build --ws",
    "lint": "npm run lint --ws --if-present",
    "release": "ts-node packages/releaser/dist/src/index.js --forceVersion=1.0.0",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "20.5.0",
    "@commitlint/config-conventional": "20.5.0",
    "husky": "9.1.7"
  },
  "workspaces": [
    "packages/*"
  ]
}
